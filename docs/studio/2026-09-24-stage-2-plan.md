# Studio — Stage 2 plan: avatars and photos

Goal: in the Studio app, create an AI avatar and generate a series of scene photos
of it — 20 photos in one run, with the expected and the worst-case cost shown
before anything is spent.

Inputs: the product concept (Claude Doc "Studio — концепция продукта"), the Stage 0
spike (`spike/studio-api/README.md`, results 2026-09-24), the Stage 1 skeleton
(`studio/`), the Stage 1 whole-slice review, and the critic review of this plan
(revision 2).

Stage 2 ships as three slices, each releasable on its own:

- **2a — engine and avatar**: engine process, API contract, money, OpenRouter client,
  library, age gate, avatar creation, Settings.
- **2b — photo run**: scene system, job queue with cancel and resume, PDQ gate,
  the Photos screen.
- **2c — face gate**: face similarity in the engine, retries on face mismatch.

## Fixed decisions

| Topic | Decision | Why |
| --- | --- | --- |
| Default image model | `x-ai/grok-imagine-image-2.0`, quality `low`, 1K, 1 reference (the master portrait) — $0.05 per photo | Best face similarity in the spike (SFace cos mean 0.77) at the lowest Grok price; 1K checked full screen by the owner |
| Identity pack | Not in Stage 2: master portrait only | A second reference added only +0.02 similarity in the spike; saves $0.12 per avatar |
| Revealing outfits | Not planned in Stage 2: revealing outfits (swimwear, sports bras, lingerie, stockings, slip dresses, robes) are disabled until the owner's provider is connected. The Glamour category stays with non-revealing outfits (mini skirt with a top, corset top; bodycon dress is untested — if Grok refuses it, the one-attempt Seedream fallback applies) | Grok refuses revealing outfits with a face reference; the Seedream fallback passed the 0.70 face threshold in only 6 of 24 spike images, so such slots would burn their attempts. Glamour-1 (mini skirt) and glamour-4 (corset) passed on every model in the spike |
| Refusal fallback | A moderation refusal (4xx, free, ~5 s) on a non-revealing scene gets one attempt on Seedream 5 Pro; per-provider face thresholds are the owner's call later | Keeps accidental refusals from failing a slot without wasting attempts |
| Scene text model | `x-ai/grok-4.3`, reasoning effort low, JSON schema output, Zod-validated | $0.011 per 25 scenes |
| Storage | JSON files: one folder per avatar, image file + sidecar JSON per photo, JSONL logs; atomic writes (temp + rename); an in-memory index rebuilt on start | `node:sqlite` works in Electron (Node 24.18) but not in bun, where the tests run; no native modules allowed |
| Ledger location | `userData/ledger.jsonl`, not the library folder | Money history must survive a moved or deleted library |
| Face check (2c) | YuNet + SFace in JS/WASM (onnxruntime-web) inside the engine, 4 threads; images decoded by Chromium (`nativeImage` in main or `createImageBitmap`), not ffmpeg. The threshold is recalibrated in 2c | `spike/face-js/` matched OpenCV to 0.0001 cosine with Chromium decoding (ffmpeg decoding drifts up to 0.03); 25 ms per image. But at 0.70 only 37 of 76 true renders pass while different people from the same description score 0.62–0.66 — options for 2c: a lower threshold that only catches gross drift, a gallery of accepted frames as references, a stronger embedding model, keep-best-of-N |
| Age check | Vision call to `x-ai/grok-4.3`: "clearly adult, at least 21"; any doubt → rejected, not retried | $0.0014 per image, 83/83 adult in the spike |
| Metadata | Library keeps generator files as received; exported media later use the `engine` signature | Owner's decision |
| Process model | main: windows, IPC, `safeStorage`, `studio-media://`; engine `utilityProcess`: OpenRouter client, queue, library, ffmpeg; heavy inference in a `worker_thread` inside the engine; renderer: UI only | Keeps the UI responsive; the key never reaches the renderer |
| Engine runtime | Every module reachable from the engine entry (`studio/engine/**`, `studio/node/**`) uses only `node:*` APIs — no `Bun.*`, no `import.meta.dir`, no `process.env` reads | The engine runs under Electron's Node while tests run under bun; green tests must mean a working package |

## Money model

Ledger lines (append-only JSONL, integer micro-dollars, `fsync` after each append):

- `reserve { attemptId, jobId, runId | avatarId, model, worstMicros, at }` — written **before** the request is sent.
- `settle { attemptId, costMicros, estimated, at }` — written after the final response of the attempt: a 2xx → its `usage.cost` (worst case with `estimated: true` if the field is missing); any final non-2xx (refusal, 4xx, 5xx after transport retries) → `costMicros: 0` (OpenRouter image billing is all-or-nothing and failed generations are not billed).
- `release { attemptId, reason, at }` — only when the request provably never reached OpenRouter (failed before `fetch`).

Rules:

- Transport retries after a non-2xx (429/5xx with `Retry-After`) stay inside one attempt id. A new attempt id is taken from the slot's pre-allocated list only after a 2xx (e.g. a QA rejection), an abort, or a timeout.
- An unsettled `reserve` counts at its worst case — in the run cap, in the global budget, and toward the slot's attempt limit — until reconciled.
- Reconcile (a user action; after a restart the app shows what needs reconciling but spends nothing): wait at least 2 minutes after the last request (`/credits` lags), compare the `/credits` usage delta since the last reconcile with the ledger total for the same window and show both, then close every open `reserve` with `settle` at its worst case and `estimated: true`, and move a torn last line to `ledger.torn`.
- A truncated last JSONL line stops all paid calls until the user reconciles.
- Two limits, checked atomically by one `Budget`: the run cap (default: the plan's worst case, e.g. 20 photos × 3 attempts × $0.055 + writer $0.03 ≈ $3.33; an avatar job's cap is its own worst case: 4 candidates + descriptor + age checks ≈ $0.23) and the global budget from Settings, counted per calendar month (UTC), default $10.
- Estimates show both expected and worst case.
- Prices come from `/api/v1/images/models/<id>/endpoints` and `/api/v1/models` (chat); a dated fallback table is used only when the fetch fails, and the UI says so.

## Invariants (tests must pin each)

1. No test can spend money: the OpenRouter client takes the key and the base URL as parameters and refuses to run without a key; tests never pass a real key; `studio/engine` and `studio/node` never read `process.env` or use `Bun.*` — a static test checks every module reachable from the engine entry (main may read `ELECTRON_RENDERER_URL` in dev only).
2. Money is integer micro-dollars; the ledger is append-only; a `reserve` is on disk before its request leaves.
3. Before any paid call: spent + unsettled reserves + in-flight worst case + this attempt's worst case ≤ the run cap and ≤ the global budget.
4. Abort, timeout or crash cost the worst case until reconciled; after a restart nothing is spent without a user click.
5. An attempt id that got a 2xx, an abort or a timeout is never sent again; only transport retries of a non-2xx (429/5xx) repeat inside the same attempt id. Every new attempt takes the next id from the slot's pre-allocated list.
6. Plan order: slots with pre-allocated attempt ids (`slot#1..#3`) are written before the writer call; prompts are written before the first image call; resume uses the plan and never re-plans.
7. At most 3 paid attempts per slot across all QA branches and fallbacks.
8. Every avatar is 21+ in its descriptor schema and every prompt; every photo passes the age check before it enters the library.
9. Reference images come only from library records of generated frames — no user file can become a face reference.
10. The API key lives only in `safeStorage` (main) and the engine's memory; never in the renderer, logs, library files, or child-process environments (engine and ffmpeg are spawned with an explicit env without `OPENROUTER_*`).
11. Library write order: image file written and `fsync`ed first, sidecar JSON second (the sidecar is the commit, also written temp + `fsync` + rename); on start, files without a sidecar go to `quarantine/`.
12. `studio-media://` serves only by id (`studio-media://photo/<avatarId>/<photoId>`): main validates both ids against `^[a-z0-9-]{8,64}$`, builds the path by the library's naming convention (`avatars/<avatarId>/photos/<photoId>.<ext>`, extension from a fixed image allowlist), checks with `realpath` that it lies inside the library root, and serves only image MIME types; registered as privileged without `bypassCSP`.
13. The base-URL override used by end-to-end tests is compiled out of packaged builds.

## Tasks

Owner of business logic, schemas and money code: `oh-my-claudecode:test-engineer` (test first).
UI and plumbing: `oh-my-claudecode:executor` / `designer`. One reviewer in a fresh context after each task; a whole-slice review at the end of each slice.

### Slice 2a — engine and avatar

| # | Task | Owner | Depends on |
| --- | --- | --- | --- |
| T0 | API contract: engine commands and events (typed, Zod), event `seq` numbers and a state snapshot on (re)connect — the window can close while the app keeps running on macOS; error model (401, 402, budget, refusal, network) | test-engineer | — |
| T1 | Engine `utilityProcess` (second vite entry, asar-safe path), transport for T0, single-instance lock, `senderFrame` checks, `safeStorage` key flow (main decrypts, hands the key to the engine over a MessagePort; `isEncryptionAvailable() === false` → the key is not stored and Settings says why), key rotation and 401 mid-run (stop, surface, never retry), `studio-media://` (invariant 12), packaged-engine smoke test | executor | T0 |
| T2 | Money core per the money model: reserve/settle/release, fsync, truncated-line stop, run cap + global budget, price fetch with dated fallback (images and chat), expected/worst estimator | test-engineer | — |
| T3 | OpenRouter client: `images` and `chat`, lenient Zod (only `data[0].b64_json` / message content required), retries on 429/5xx with `Retry-After`, 180 s timeout, raw body saved on an unusable paid 2xx, `/credits` reconcile. Port `openrouter.ts`, `jobs.ts`, `image.ts`, `files.ts`, `config.ts` from `spike/studio-api/lib/` with the runtime rule applied and `ffmpegPath()` for decoding | test-engineer | T2 |
| T4 | Library store: avatar manifest (traits, descriptor, master ref, status, created; on-video text is English only, so no per-avatar language), photo record = image + sidecar, write order and startup reconciliation with quarantine (invariant 11), in-memory index, `usedIn` as an append-only log, run folders (plan, journal), per-avatar scene history, thumbnails via ffmpeg, schema version field | test-engineer | — |
| T6a | Avatar jobs: descriptor from traits (LLM), 4 candidate portraits, pick → master, age gate on candidates; uses T2/T3/T4. Normalise the LLM descriptor (diacritics, dashes) before `studio/shared/engine/ageText` validation so a paid answer like "café-style" is not dropped; extend ageText for the gaps the T0 review left open (underaged, adolescent, pubescent, juvenile; "she's N", "she is sixteen", "at N", "N years young", "ages N-M"; старшеклассница, …классница, "ей всего N"). Contract additions requested by the T8a and T1 reviews: `MoneyStatus` halt cause (settle-above-worst, ledger-*); `ReconcileResult` gains `mismatch`, nullable `creditsDeltaMicros`, `aboveWorstAttempts`; events `job.cancelled`, `settings.changed` (incl. key status `rejected`), `avatar.changed` / `draft.changed`; an estimate for "4 more candidates" on an existing draft without the descriptor call. Carried over from the T1 review: the key-status event replaces the `engine.error AUTH_INVALID` that `markKeyRejected()` emits today; a `Snapshot` field with pending engine notices (engine crash, corrupt `settings.json`) so a window opened after the notice still shows it; `Budget.setMonthlyBudget` under the budget's own mutex instead of rebuilding `Budget` on a budget change (a rebuild forgets which open reserves are this process's own) | test-engineer | T0, T2, T3, T4 |
| T8a | UI: Avatars grid, New avatar wizard (traits → 4 candidates → pick), Settings (key with rotation, model, network concurrency, global budget, library folder); against the T0 contract | designer + executor | T0 |

Done when: a packaged build on macOS and Windows creates an avatar end-to-end against the mock server; a real canary (4 candidates, ≈ $0.20) passed with the owner's approval and the `/credits` delta was ≤ the ledger total; invariants 1–5 and 8–13 are pinned by tests.

### Slice 2b — photo run

| # | Task | Owner | Depends on |
| --- | --- | --- | --- |
| T5a | Scene planner: pools as data, seeded planner (location-bound times and activities, mirror only at mirror locations, photoshoot deck, no revealing outfits in Stage 2), exclusion of the avatar's recent location + outfit pairs, pre-allocated attempt ids | test-engineer | — |
| T5b | Scene writer + assembler: writer prompt v2 (one hand holds the phone in selfie and mirror shots; full sentences; youth-word refine; no children), assembler (reference binding, anchors from the descriptor, shot phrase, realism suffix, constraints, stop-words) | test-engineer | T2, T3 |
| T6 | Job queue: network pool (default 6, shrinks on 429) and CPU pool, plan order (invariant 6), cancel (abort in-flight → worst case until reconciled), resume after a crash, per-slot attempt limit, provider route with the one-attempt Seedream fallback, progress events per T0; declares the `QaGate` interface; changing the library folder is refused with `IN_FLIGHT` while jobs run | test-engineer | T2, T3, T4, T5a |
| T7a | QA gates for 2b: age gate on every photo, PDQ near-duplicate check (reuse `src/core/pdq` read-only; decode via `ffmpegPath()`), thresholds in one config | test-engineer | T6 |
| T8b | Photos screen: count, categories, resolution, live expected/worst cost, scene review table, gallery with statuses, cancel and resume | designer + executor | T0, T5a |

Done when: after `kill -9` in the middle of a 20-photo run and a resume, no attempt id was sent twice, the `/credits` delta is ≤ the ledger total, and the run cap was not exceeded; cancel works; the estimate shows expected and worst; a real canary (20 photos ≈ $1.05, cap $3.33) passed with the owner's approval.

### Slice 2c — face gate

| # | Task | Owner | Depends on |
| --- | --- | --- | --- |
| T7b | Face gate implementing `QaGate`: YuNet + SFace via onnxruntime-web (WASM, 4 threads) in the engine, pixels decoded by Chromium and passed to the engine, `.onnx` and `.wasm` files shipped in `out-studio` and unpacked from asar, `wasmPaths` set explicitly, threshold strategy chosen at the start of 2c from the options above, head-size ratio, retries on mismatch within the attempt limit, installer size (+≈ 53 MB) tracked in CI | test-engineer | T6, face spike (done) |

Done when: parity with the OpenCV numbers on the spike image set (≤ 0.001 cosine); the chosen threshold strategy keeps most true renders and rejects the spike's impostors; the gate is on in a real run.

## Open questions

- The owner's provider for revealing scenes: API, parameters, reference support, pricing, face threshold.
- Whether OpenRouter bills requests cut off by the client (its docs say no): verify on the 2a canary by reconciling `/credits`.
- Face check implementation: result of `spike/face-js/`.

## Out of scope for Stage 2

Montage and rendering (Stage 3), autopilot (Stage 4), music, GIFs, the Multiply button, the identity pack.
